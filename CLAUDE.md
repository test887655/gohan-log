# 食事・レシピ・食材管理アプリ 設計メモ

## 目的
自分と姉の2人だけで使う非公開アプリ。ストア公開はしない。
- 毎食の写真を送り合っている今の習慣を、アプリ上の共有タイムラインに置き換える
- レシピを作った時の写真ごと保存する
- 家の食材を管理して食品ロスを減らす

## 前提・制約
- 利用端末：iPhone（2人とも）。Macは持っていない
- 形式：PWA（Webアプリ）。SafariでURLを開き「ホーム画面に追加」して使う
- 開発環境：Windows/PC + Claude Code
- ホスティング：GitHub Pages
- データ保存：Supabase（無料枠）。Auth・Database・Storage を使う
- ユーザーは eri・hana・minami・mihoko の4アカウント。新規登録は受け付けない（Supabase側でサインアップを無効化）。
  アカウントはダッシュボードの Authentication → Users から手で足す
- 写真はSupabase Storageに保存。アップロード前にクライアント側で縮小（長辺1200px程度）して容量を抑える
- 技術スタックはシンプルに。ビルド不要か最小限（例：素のHTML/JS、またはVite + 軽量フレームワーク）。GitHub Pagesで動くこと

## 共有ルール（重要）
共有は「相手どうし（partners）」の組で決まる。
- eri … hana・minami・mihoko の3人と組む
- hana … eri・mihoko の2人と組む
- mihoko … eri・hana の2人と組む
- minami … eri だけと組む
組んでいない人どうし（hana と minami、mihoko と minami）はお互いに見えない。
- 自分の投稿は、組んでいる相手みんなに見える（eri の投稿は3人とも見える）
- ごはん記録（タイムライン・アルバム・お店）は、同じ輪の人をまとめて並べる。
  輪は app.js の CIRCLES に表示名で持つ（['eri','hana','mihoko'] と ['eri','minami']）。
  partners は自分の行しか読めず「誰と誰が組んでいるか」が分からないため、
  データベースではなくコードに持たせている。人を足すときは CIRCLES にも足すこと。
  eri が hana を選んでも mihoko を選んでも、出るのは eri・hana・mihoko の3人ぶん。
  minami は別の輪なので、minami を選んだときだけ eri と minami の2人ぶんになる
- 何も選んでいないときは、いちばん人数の多い輪の相手を選ぶ（biggestCirclePartner）。
  eri が開いたとき、切り替えなしで3人ぶんが出るようにするため
- ヘッダーの「共有する相手」を出すのは、輪が2つ以上ある人（eri）だけ。
  hana・mihoko は切り替えてもごはん記録が同じ3人ぶんなので、ホームには出さない。
  ただし、そうだんは宛先が変わるので、そうだんの画面では相手が2人以上いる人みんなに出す
- プレゼントの相手は、ポップアップの中の切り替え（#gift-partner-picker）で選ぶ。
  ヘッダーの切り替えは、ポップアップの後ろに隠れて押せないため。
  選んだ相手は app.js の setPartner に渡して、ヘッダー側と食い違わないようにする
  変わるのは eri の画面にどちらの相手のぶんを出すかと、そうだんの宛先。
  選んだ相手は端末ごとに localStorage（partner:<自分のid>）へ覚える

| 機能 | 共有 |
|---|---|
| 食事ログ | 組んでいる相手と共有（自分と、選んでいる相手の投稿が同じタイムラインに並ぶ） |
| お店（外食の記録） | 組んでいる相手と共有（誰が書いたか名前で分かる） |
| そうだん | 聞いた人と聞かれた人の2人だけ |
| 食材リスト | 各自のみ |
| レシピ帳 | 各自のみ |
| 買い物リスト | 各自のみ |

SupabaseのRow Level Securityで実装する：
- 見てよいかは関数 public.can_see(owner) で判定する（本人か、partners に組がある人）。
  meals / meal_reactions / places / profiles / 写真（Storage）の閲覧はこれを使う。
  作成・編集・削除は本人のみ
- chats / chat_messages：chats の user_id（聞いた人）か partner_id（聞かれた人）だけ。
  聞けるのは partners に組がある相手だけ
- gift_items：用意した人（user_id）と、向けた相手（partner_id）だけが読める。
  作れるのは partners に組がある相手に向けたものだけ
- chat_seen：本人のみ（相手ごとに、最後に開いた時刻を持つだけ）
- partners：自分の行だけ読める。書き換えはダッシュボードのSQLからだけ行う
- 相手を足すときは、profiles に表示名、partners に両向き2行を入れる
- ingredients / recipes / shopping_items / ingredient_edits：本人のみ

## 機能
### 1. 食事ログ（最優先）
- 写真（任意）＋メニュー名（列は note。画面の表記だけ「メニュー名」にした）＋日時＋朝/昼/夜/間食の区分
  ＋「ジャンル」（食べ物/飲み物/その他。列は category で text[]。複数選べるが1つは必ず選ぶ。
    チェックボックスなので required は効かず、保存のときにJSで確かめる）
  - カードではメニュー名のすぐ上に「● 食べ物」「● 飲み物」のように札（.category-tag）を並べる。
    並びは選んだ順ではなく、いつも 食べ物→飲み物→その他。
    丸の色はどれも差し色でそろえる（以前は飲み物・その他を別の色にしていたが、色分けはやめた）
  - 撮り忘れた食事も残せるよう、写真なしでも投稿できる。写真なしの記録は
    テキストだけのカードとして表示し、あとから編集で写真を追加できる
  - 日記（任意、100文字まで）。ごはんのメモとは別に、その日の気分やできごとを書く
    （「今日は忙しくてお惣菜多め」「ダイエット中。目指せ-5キロ！」など）。
    書いたときだけ、カードのメモの下に「日記」と付けた薄い色の帯で出す
  - お店の名前（任意）。外食したときに残せる。書いたときだけ
    カードに地図のピンのマークを付けて出す。家で食べたときは空のまま。
    登録済みのお店から選べる（下の「お店」を見る）
- 2人分が時系列で並ぶタイムライン。誰の投稿かわかる表示
- 表示は1週間ずつ（月曜〜日曜）。前後の週に移動できる。一度に読む量を抑える狙いもある
- 自分の投稿は編集・削除可
- 相手の投稿には「いいね」「おいしそう」「レシピが知りたい」を押せる。押すと色がつく
  - 自分の投稿にはボタンを出さず、「◯◯さんがいいね」と名前で表示する
- 自分の記録には★（お気に入り）を付けられる。うまくできた記録を自分で認めるためのもの。
  相手には出さない。付け外しは編集・削除と同じ行のボタンから
- 写真だけをタイルで並べるアルバムがある。週の並びの下の「アルバム」から開く。
  2人ぶんが新しい順に3列で並ぶ。写真のある記録だけ。タイルを押すと、その週の
  タイムラインへ移る。通信を増やしすぎないよう18枚ずつ読み、続きは「もっと見る」
  - 外食したお店をためる「お店」の画面がある。週の並びの下の「お店」から開く。
    名前とメモ（感想）を書ける。2人で共有して見られ、誰が書いたか名前が出る。
    直せる・消せるのは書いた人だけ。記録するときの「お店の名前」は、
    ここに登録したお店から選べるので、毎回打ち直さなくていい
  - ★だけを集めた画面がある。週の並びの下にある「★ お気に入り」から開く。
    戻るのは食材リストと同じお茶碗。写真の通信を増やしすぎないよう12件ずつ読み、
    続きは「もっと見る」で足す。ここで★を外すと、その場からカードが消える

### 2. 食材リスト
- 食材名・数量・単位・賞味期限・保存場所（冷蔵/冷凍/常温）
  - 数量もボタンから選ぶ（なし・1〜10・1/2・1/4・任意）。「任意」を選ぶと自由入力欄が
    出て、11以上や「少々」のような書き方も入れられる。日本語を打てるよう type は text
  - 単位はボタンから選ぶ（なし/個/g/ml/本/枚/袋/パック/玉/束/缶）。
    毎回打つのが面倒なので自由入力にはしない。
    一覧にない単位で登録済みのものは、編集時にそのボタンを足して選べるようにする
- 賞味期限が近い順に並ぶ。期限が3日以内は目立たせる
- 一覧は1品目1行に詰めて、全体を見渡せるようにする
- 名前を押すと編集フォームが開く。削除は各行のゴミ箱アイコンから行う
- 各行の − ＋ で、フォームを開かずに数量を増減できる。
  連打されても通信が増えないよう、0.5秒待ってから最後の1回だけ送る。
  「1/4」のような分数は分母をそのままに 1/4→2/4→3/4→4/4 と動かす（4/4より上と
  1/4より下には行かない）。「少々」のように数で書かれていないものには − ＋ を出さない
- 「＋ 食材を追加」のすぐ下に、最後に直した日時を小さく右寄せで出す。
  足した・消した・− ＋ で数を変えた・フォームで直した、のどれでも今の時刻に更新する。
  消した行は無くなってしまうので、日時は ingredient_edits に1人1行で持つ
- 使い切ったら削除、または数量を減らす

### 3. レシピ帳
- タイトル・材料（名前＋分量）・手順・メモ・写真（複数可）
- 「作った」記録：作った日と写真を追加できる

### 4. 連動・買い物リスト
- レシピを開くと、食材リストと照合して「足りない材料」を表示
- 足りない材料を買い物リストに一括追加
- 食材リストから「今ある食材で作れるレシピ」を表示
- 材料名の表記ゆれ対策：最初は登録済みの食材名からの選択式にする。自由入力は後回し

### 5. ポイントとプレゼント
- 毎日開きたくなるための仕掛け。朝（5:00〜11:59）昼（12:00〜16:59）夜（17:00〜4:59）の
  3回、ホーム画面のどこかにキラキラ（ダイヤ2つ）が出る。押すと3ポイント
  - 夜は日付をまたぐので、0:00〜4:59は前の日の夜として数える（1日は朝5時に変わる）
  - 出る場所は毎回変える。食材リストを開いているあいだは出さない
- 3日に一度は星が出て、ボーナス10ポイント（キラキラの3＋10で13ポイント）。
  星の日は3日ごとで変えない。朝・昼・夜のどれに出るかだけをでたらめに決める
  （points.js の bonusSlot）。前の星から1つか2つ進めるので、同じ時間帯が2回続くことはない。
  以前は「その日の最初のひとつ」を星にしていたが、朝に開く人はいつも朝になってしまった。
  2人とも同じになるよう、日付そのものから決める（端末ごとの乱数は使わない）。
  星の時間帯に開けなかったときは、ほかの時間帯はふつうのキラキラ（3ポイント）のまま
- 朝・昼・夜の3食を記録した日は、ボーナス10ポイント。1日1回だけ。
  間食は数に入れない。記録した瞬間と、開いたときに見に行く。
  どの日のぶんかは食べた日時から決めるので、あとから日付を戻して記録してもよい
  - 数えるのは「写真つき」でジャンルに「食べ物」が入っている記録だけ
    （points.js の checkMealBonus。category に 'food' を含む＝ contains）。
    食べ物＋飲み物のように組み合わせていれば入る。
    写真なしや、飲み物・その他だけの記録は、記録はできるがボーナスに入れない。
    文字だけの「水」を朝ごはんにして稼ぐ抜け穴をふさぐため（2026-09-22）
  - 入るのは今日と昨日のぶんだけ。昨日を入れるのは、夜ごはんを翌朝に付けることがあるため。
    それより前の日は、記録はできるがボーナスは付かない（過去を埋めて稼がないようにする）
- ポイントはタイトルの横に出し、プレゼントのマークを押すと交換の画面が開く
- プレゼントは**贈る側が内容とポイント数を決めて並べ、相手が選ぶ**。
  プレゼントは相手ごとに別々に用意する（eri は hana 向けと minami 向けを分けて作る）。
  「用意するもの」「もらえるもの」とも、選んでいる相手のぶんだけ出す。
  ポイントは相手で分けず、1人1つの合計
  減るのは選んだ側のポイント。用意した側は減らない
- ガチャ：ヘッダー下の冷蔵庫・吹き出しの横にあるガチャガチャのアイコンから開く。
  プレゼントの中ではなく、ガチャだけの画面にして大きく見せる。1日1回だけ引けて、ポイントが当たる。
  出るのは 1（35%・🍬）3（30%・🍪）5（25%・🍰）10（9%・👑）100（1%・💎 大当たり）。
  絵は GACHA_PRIZES の face。画面の「出るもの」の一覧もこの表から作るので、
  中身を変えるときは GACHA_PRIZES だけ直せばよい。
  出す人は points.js の GACHA_USERS（表示名で判定。今は eri・hana・mihoko。
  minami にはアイコンごと出さない）。
  1日1回は points の (user_id, claim_day, slot) の一意（points_one_per_slot_idx）で守る。
  kind も slot も 'gacha'。日の区切りはキラキラと同じ（朝5時）
- ガチャの演出はルーレット。当たりの絵だけを順に見せ（1秒は必ず回す）、
  だんだん遅くして（0.15→0.21→0.29→0.38秒）当たりの絵で止める。
  止まると絵がぽんと弾んでキラキラがはじけ、そのあとに数字とボタンの「おしまい」、
  「また明日♪」を出す（回っている途中で先に出すとネタバレになる）。
  回っているあいだは「なにが出るかな…」のまま、当たりが出るときに入れ替える。
  当たりの文字の場所は2行ぶんの高さで先に取っておく。高さが変わると
  （画面の真ん中にそろえている関係で）ボタンが上下に動いてしまうため。
  キラキラは枠からはみ出すので .gacha を overflow: hidden で切る。
  切らないと画面の下に横スクロールの棒が出る。
  大当たりだけキラキラが倍で🎉も混ざり、文字が点滅して枠も光る。
  「視差効果を減らす」を選んでいる端末では動かさない（prefers-reduced-motion）
- 相手が選ぶと、用意した人が次に開いたときにポップで知らせる
- プレゼントの画面は、相手が用意したもの（もらえるもの）に色を付けて、自分が用意したものと
  見分けられるようにする（.gift-item.theirs）。1件ずつの余白は詰めすぎない。
  数が増えたらポップアップの中だけスクロールする
- ポップアップ（プレゼント・お知らせ）を開いているあいだは、後ろのごはん記録を動かさない。
  body に modal-open を付けて位置ごと固定し、閉じたときに元の高さへ戻す
  （iOSは overflow: hidden だけでは止まらない）

### 6. そうだん
- 食材リストの各行にある吹き出しを押すと、その食材のことを相手に聞ける。
  文は決まっていて「◯◯料理教えて！」。その下に任意で足したいことを書ける
  （書くと2行目として一緒に送られる）
- やりとりの画面は、ヘッダー下の冷蔵庫の横にある吹き出しから開く。どの画面からでも押せる。
  開いているあいだは、冷蔵庫と同じように吹き出しに色が付く（aria-pressed="true"）。
  ごはんへはタイトル、食材リストへは冷蔵庫で戻る
- 食材以外のことは、そうだんの画面の「＋ 聞きたいことを書く」から自由に書ける
- 返事はその場の欄から。スタンプ（👍🙏😋🍳💡🤔）は押すとすぐ送る
- スタンプの右のゴミ箱で、そのそうだんをまとめて消せる（返事も一緒に消える）。
  出るのは自分が始めたそうだんだけ。狭い画面でも1行に収まるよう、
  スタンプのほうが縮む作りにしてある
- 聞く相手は、そのとき選んでいる相手（minami は相手が1人なので迷わない）。
  一覧も選んでいる相手とのぶんだけ出す
- 相手からの新しい書き込みがあると、冷蔵庫のアイコンに赤い丸が出る。
  eri は「共有する相手」のボタンにも、書き込みがあった相手のほうに丸が出る。
  冷蔵庫はどの画面にも出ているので、ごはんの画面からでも気づける。
  そうだんの画面を開くと丸は消える（最後に開いた時刻を覚えておく作り）
- そうだんの画面だけは開くたびに読み直す。新しい返事に気づけないと意味がないため。
  文字だけなので通信は軽い。一度に読むのは新しい20件まで

### 見た目
- 「シンプル」「大人」「かわいい」「水族館」「動物園」「ダーク」の6種類。
  ふだんは冷蔵庫・吹き出しと同じ列の右はしにある「きせかえ」だけを出しておき、
  押したときにその下へ並べる（狭い画面で入りきらなければ次の行へ回る）。
  選んでいるものに色が付くのは今までどおり
  - シンプル：既定。オレンジ系。data-theme を付けない状態
  - 大人：くすんだローズ系。お花は線画、壁紙も控えめ
  - かわいい：ピンク系。お花は塗りつぶし、壁紙もはっきりめ
  - 水族館：水色系。元の絵は背景を抜いて生き物ごとに切り出し、小さくして使っている
    （images/ の aquarium-*.png と whale.png。どれも2倍の大きさで作ってCSSで半分に縮める）。
    スマホでは写真のカードが幅いっぱいに並ぶので、全体に散らすとカードの裏に隠れて見えない。
    そこで、必ず空いている場所にだけ置く：
    左右のふち20pxに小さな生き物を縦に並べる（詰めすぎると多く見えるので、480pxに6匹ほど）（aquarium-left / right、repeat-y）、
    冷蔵庫・吹き出しと「きせかえ」のあいだにカメとクマノミ（.toolbar-deco）、
    タイトルの横はお花の代わりにクジラ（.sea-title）。
    タイトルとログアウトのあいだにイルカ（.header-deco）、
    「◯◯さんとして記録中」の右（きせかえの下）にアザラシ（#greeting::after）、
    「記録する」と週の並びのあいだ（28px）とカードどうしのすきま（20px）にも、
    すきまに収まる大きさの小さな生き物（aquarium-gap*.png、::before で置く）。
    幅481px以上の広い画面だけ、全体に散らした壁紙（aquarium-bg.png）にする。
    タイトルのすぐ後ろにクジラが来ないよう、少しずらして敷いている。
    壁紙は --page-pattern（-size / -position / -repeat）で、カンマ区切りで何枚でも重ねられる。
    画像を差し替えたら sw.js の CACHE も上げる
  - 動物園：クリーム色の地に茶色の差し色。水族館と同じ作りで、元の水彩の絵から
    1匹ずつ切り出して背景を抜いてある（images/ の zoo-*.png と elephant.png。
    どれも2倍の大きさで作ってCSSで半分に縮める）。置き場所も水族館と同じで、
    左右のふち20pxに小さな動物（zoo-left / right、repeat-y。
    160pxおき＝表示80pxおきに6つ。あけるとすき間が目立つので詰めて並べる。
    左のいちばん上だけは草にする。タイトルの横のぞうの隣なので、
    動物を置くとごちゃごちゃして見えるため）、
    冷蔵庫・吹き出しと「きせかえ」のあいだにライオンと草（.toolbar-deco）、
    タイトルの横はクジラの代わりにぞう（.zoo-title。横に広いので幅は1.9em）、
    タイトルとログアウトのあいだにキリン（.header-deco）、
    「◯◯さんとして記録中」の右にうさぎとミーアキャット（#greeting::after）、
    「記録する」と週の並びのあいだとカードどうしのすきまにも小さな動物（zoo-gap*.png）。
    幅481px以上の広い画面だけ、全体に散らした壁紙（zoo-bg.png）にする
    - ガチャのアイコンが出ている人は .toolbar-deco の空きが88pxしかない。
      zoo-toolbar.png と aquarium-toolbar.png はそこに収まる約176px（表示88px）で作ってある。はしで動物が切れるため。水族館はカメの右に小さめのクマノミを少しあけて置いてある
    - 壁紙は上の帯（表示95pxぶん）を草だけにして、タイトルや「◯◯さんとして記録中」の
      後ろに動物が来ないようにしてある。重なると字が読みにくい
    - 水族館と動物園は置き場所が同じなので、CSSは大きさ・位置をまとめて書き、
      絵（background-image）だけ見た目ごとに差し替えている
  - ダーク：暗い背景に落ち着いた琥珀色。お花も壁紙も出さない。目が疲れないことを優先
- 選んだ見た目は端末ごとに localStorage へ保存。2人がそれぞれ好きなものを選べる
- お花はSVGの symbol を使い回す。塗るか線で描くかも変数（--flower-fill など）で切り替える
- 色・角丸・影・壁紙はすべてCSS変数。見た目を足すときは :root[data-theme="○○"] を1つ増やすだけ
- ちらつき防止のため、テーマの復元は index.html の head 内インラインscriptで行う
- ダークは color-scheme: dark も指定する。ないと日時や写真選択の枠が白いまま残る
- 古いiOSでも崩れないよう color-mix() は使わない。薄い色は変数（--like-tint など）で持つ

## 作る順番
1. プロジェクト雛形 + Supabase接続 + ログイン（2アカウント）
2. 食事ログ（この時点で姉と使い始める）
3. 食材リスト
4. レシピ帳
5. 連動・買い物リスト

## データモデル案
- profiles (id, display_name)
- meals (id, user_id, photo_path, note, place, eaten_at, meal_type, created_at)
  - photo_path は NULL 可（写真なしの記録）
  - place は外食したお店の名前。NULL 可（40文字まで）
  - diary は日記。NULL 可（100文字まで。制約 meals_diary_length）
  - favorite は自分で付ける★。true / false（既定は false）
  - category は text[]。中身は 'food' / 'drink' / 'other' のどれかで、1つ以上
    （check 制約 meals_category_check）。既定は '{food}'。
    列を足す前の記録はぜんぶ食べ物として扱う
- meal_reactions (id, meal_id, user_id, kind, created_at)
  - kind は 'like'（いいね）'yummy'（おいしそう）'recipe'（レシピが知りたい）
  - (meal_id, user_id, kind) が一意。押す＝insert、取り消す＝delete
  - meals と同じ共有ルール：2人とも閲覧可、押す・取り消すは本人のみ
- places (id, user_id, name, note, created_at)
  - 外食したお店。name は40文字まで、note（メモ・感想）は200文字まで。NULL 可
  - 2人とも閲覧可。作成・編集・削除は本人のみ（meals と同じ）
- partners (user_id, partner_id)
  - 共有する組。両向きで2行入れる（eri→hana と hana→eri）。主キーは2列の組
- chats (id, user_id, partner_id, topic, created_at)
  - partner_id は聞かれた人
  - ひとつの相談のまとまり。topic は食材名（30文字まで）。
    「＋」から書いた自由な相談は NULL
- chat_messages (id, chat_id, user_id, body, stamp, created_at)
  - body は文（200文字まで）、stamp は絵文字。どちらか一方は必ず入る
- chat_seen (user_id, partner_id, seen_at)
  - 赤い丸を出すためだけの表。最後にそうだんを開いた時刻を、相手ごとに1行で持つ
    （主キーは user_id と partner_id の組）。
    seen_at より新しい「その相手の書き込み」があれば丸を出す
- ingredient_edits (user_id, edited_at)
  - 食材リストを最後に直した日時。本人だけが読める。
    書き込みは関数 touch_ingredients()（security definer）から行う。
    この関数が auth.uid() の行を作る／今の時刻に直して、その日時を返す
- ingredients (id, user_id, name, quantity, unit, expires_on, storage, created_at)
  - quantity は text（「半分」なども入れられるようにするため。10文字まで）
- recipes (id, user_id, title, steps, note, created_at)
- recipe_ingredients (id, recipe_id, name, quantity, unit)
- recipe_photos (id, recipe_id, photo_path, cooked_on)
- shopping_items (id, user_id, name, quantity, unit, done, created_at)
- points (id, user_id, kind, amount, claim_day, slot, created_at)
  - kind は 'sparkle'（キラキラ）'star'（ボーナス）'meals'（3食そろった）
    'gift'（使った）'gacha'（ガチャ）。使うと amount はマイナス。
    slot は 'morning' / 'noon' / 'night' / 'meals' / 'gacha'。
    どちらも check 制約で決まっているので、種類を足すときは制約も直す
  - (user_id, claim_day, slot) が一意。同じ時間帯で二度は取れない。
    3食ボーナスも slot を 'meals' にして、この一意制約に乗せている
  - 残りは my_points() 関数で合計だけ受け取る（全行を持ってこない）
  - 直す・消すのポリシーは作らない。あとから書き換えられないようにするため
  - 不正対策（2026-09-22、sql/points_guard.sql）。アプリから送る amount を信用せず、
    insert のたびにトリガー points_guard が検査する。
    キラキラは「今」の時間帯だけ（過去・未来は入れられない）で 3、星は 13 で
    星の時間帯（gohan_star_slot。points.js の bonusSlot と同じ計算）のときだけ。
    3食ボーナスは 10 で、今日か昨日のぶんだけ、しかも写真つきの食べ物の朝・昼・夜が
    その日にそろっているときだけ。
    ガチャは draw_gacha()、プレゼントは choose_gift(item_id) の関数からしか入れられない
    （関数の中で gohan.inside を立てて、それをトリガーが見る）。
    ガチャの当たりは関数の中の random() で決める（確率を変えるときは points.js の
    GACHA_PRIZES と draw_gacha の両方を直す。GACHA_PRIZES は絵と一覧の表示用）。
    プレゼントはポイントを引くのと gifts への知らせを1つのまとまりで行い、
    引く数は gift_items の cost から取る。足りないときは 'not enough points' で失敗。
    日付・時間帯の計算は日本時間（Asia/Tokyo）で行う（gohan_now / gohan_day / gohan_slot）
- gift_items (id, user_id, partner_id, name, cost, created_at)
  - partner_id は誰に向けたプレゼントか。読めるのは用意した人と向けた相手だけ。
    作成・編集・削除は本人のみ（用意した人だけが直せる）
- gifts (id, chosen_by, offered_by, name, cost, created_at, seen_at)
  - chosen_by は選んだ人、offered_by は用意した人。seen_at は知らせを見た時刻
  - 閲覧は2人とも、作成は選ぶ人、seen_at の更新は用意した人のみ

## 進め方の希望
- 開発者は初心者寄り。Supabaseの設定など自分で行う作業は、手順を1つずつ具体的に示すこと
- 一気に全部作らず、段階ごとに動作確認しながら進める

## 進捗（2026-09-16 時点）
- 済：ステップ1（雛形・Supabase接続・ログイン）
- 済：ステップ2（食事ログ）＋ PWA化 ＋ GitHub Pages公開
- 済：ステップ3（食材リスト）
- 済：ポイントとプレゼント
- 済：★お気に入り・アルバム・「おいしそう」・外食の記録（お店）
- 済：そうだん（食材から相手に聞く・スタンプ・赤い丸のお知らせ）
- 済：3人目（minami）・4人目（mihoko）の追加。共有の組（partners）と相手切り替え
  （mihoko は eri と hana の2人と組む。2026-09-20 に追加）
- 次：ステップ4（レシピ帳）。ただし手入力前提ではなく、ネットのレシピのURLを
  貼って取り込む形にしたいという希望あり（保留中）

### 環境
- 公開URL：https://test887655.github.io/gohan-log/
- リポジトリ：test887655/gohan-log（公開。無料プランではPages利用に公開が必須のため）
- Supabaseプロジェクト：atvrunfaltnkbwhtpoue
- アカウント：eri / hana / minami / mihoko。新規登録・メール確認は無効化済み
- ローカル確認：`python -m http.server 5173`（Node.jsは未インストール）

### 決まったこと・注意点
- SupabaseのSQLはブラウザ操作で実行できる（EdgeでSupabaseにログイン済み）。
  publishable キーではDDLを実行できないため、テーブル追加時はこの方法を使う。
  Runボタンは押しても効かないことがある。エディタの中を1回クリックしてから
  Ctrl+Enter を押すほうが確実。
  SQLのタブが裏に隠れている（別のタブが表・ウィンドウが最小化）と「Running...」のまま
  結果が返ってこない。そのときは使っているSQLのタブを表に出してもらう
- テーブルの user_id は列の既定値（default auth.uid()）で入れている。
  insert のときに user_id を送っていないので、新しい表を足すときは
  この既定値を忘れずに付ける。ないと not null で保存できない
- RLSは編集・削除の失敗時にエラーではなく「0件」を返す。
  update / delete には必ず `.select()` を付け、0件を失敗として扱うこと
- ログインは端末に覚えたままにする（supabase.js で persistSession と
  autoRefreshToken を書いてある。storageKey は既定のまま。変えると、今覚えている
  ぶんが読めなくなって全員ログインし直しになる）
  - 開いた直後は screen-boot（読み込み中…）を出し、覚えているかが分かってから
    ログイン画面かごはんの画面へ切り替える。いきなりログイン画面を出すと、
    覚えているのに切れたように見えてしまう。返事が来ないときは8秒でログイン画面にする
  - 画面に戻ったときの作り直し（refreshSession）は、圏外で延ばせなかっただけなら
    ログイン画面に戻さない。覚えているぶんは端末に残っていて、つながれば続けられる
  - iOSはしばらく使わないと覚えたログインを消すことがあるので、
    navigator.storage.persist() で消さないよう頼んでいる
- Service Workerの fetch には `cache: 'reload'` が必須。
  ないとGitHub Pagesのキャッシュ指示で更新が最大10分届かない
- 写真は非公開バケット。表示は1時間の署名URLで行い、
  タブ復帰時（visibilitychange）にセッションごと作り直す
- 食材リストはヘッダー下の冷蔵庫アイコンで開く。タブは置かない。
  そうだんはその横の吹き出しから開く。アイコンが2つ並ぶぶん狭い画面では
  見た目の列が次の行へ回る（.toolbar を flex-wrap: wrap にしてある）。
  冷蔵庫だと分かるよう、ここだけは見た目に関係なく実物の色で塗る。
  上が冷凍室・下が冷蔵室の2ドアにして、取っ手を濃いめに描くと冷蔵庫に見える。
  ごはんへ戻るのは左上のタイトル「ごはん記録」。どの画面からでも押せるので、
  画面ごとに戻るボタンは置かない（お茶碗のアイコンは役目が終わったのでやめた）。
  冷蔵庫の色は style.css ではなくSVGの fill 属性に直接書く。
  CSSだけ古いまま届くと、塗り指定が無い絵は真っ黒で表示されてしまうため。
  ごはんの画面は今のまま変えない。開いた画面だけ読み込み、
  一度読んだ画面は切り替えても読み直さない（通信を減らすため）
- アイコンだけのボタンは .icon-button で囲む（48px四方）。
  冷蔵庫とお茶碗は丸（.icon-button.round）。行き来する対の2つなので形をそろえる。
  ログアウトは四角のままにして、役目の違いが分かるようにする。
  四角の角の丸みは --radius-input なので、見た目を変えると一緒に変わる
- 使い方（チュートリアル）はヘッダーのログアウトの左にある丸い「？」から開く。
  中身は help.js の TOPICS に全部書いてある（絵は写真ではなく、アプリの部品を並べたミニ画面。
  きせかえを変えても同じ色になる）。項目は「ポイントのもらい方」（中に キラキラと星／
  3食そろえてボーナス／つかない例）・プレゼントの作り方・交換・きせかえ・食材リスト・
  そうだん・戻り方・ログアウト。ガチャは入れない。
  説明を直すときは help.js だけ直す。後ろを止めるのはパスワードと同じ setOtherPanel
  初めて開いたときは自動で1回だけ開く（showHelpOnce。見たかは localStorage の
  help-seen:<自分のid> に端末ごと・人ごとに覚える）。ログアウトすると忘れる（forgetHelpSeen）ので、
  次にログインしたときにもう一度1回出る。いちばん上に合言葉（.help-concept）を出す
- パスワードは、アプリの中から自分で変えられる。「◯◯さんとして記録中」のすぐ下の
  「パスワードを変える」から開く。新しいものを2回入れて保存（supabase.auth.updateUser）。
  変わるのはログインしている本人のぶんだけ。Supabaseのダッシュボードや
  メールのリンク（Site URL が既定のままなので localhost に飛んでしまう）を使わずに済む。
  後ろのごはん記録を止めるのは、プレゼントと同じ仕掛け（points.js の setOtherPanel）
- ログアウトは文字ではなく、灰色の開いた扉のアイコン。
  絵だけのボタンなので、押すと確認のポップアップを出す。
  灰色の線画では小さくするとドアに見えなかった。冷蔵庫・お茶碗と同じで、
  色を付けると小さくても何の絵か分かる。
  形はフリー素材の絵を測って合わせた。上下に横木を渡すと26pxでもドアだと分かる。
  戸口の中は塗らない。背景が透けて、奥が見えている形になる。
  灰色は明るい画面でも暗い画面でも見える濃さにしてある（枠 #9a9289／扉 #8b8379）
- お知らせの赤い丸（.dot）は .icon-button の右上に重ねている。
  .icon-button に position: relative が要る。ないと画面の隅に飛んでいく
- SVGの symbol の id と、ボタンなどの id をぶつけないこと。
  同じ id があると use の参照先がボタンのほうになり、絵が出なくなる
  （#sparkle のボタンは sparkle-button にしてある。
  アルバムの絵も、写真の入力欄と同じ photo にしてしまい投稿が止まった。
  今は photo-icon。絵のほうの名前を変えて避けている）
- ホーム画面から開いたアプリにはブラウザの「引っぱって更新」が無いので、自前で付けてある。
  画面のいちばん上で下へ110px引っぱって離すと（70pxでは軽すぎた）、ページごと読み直す（新しい記録も、
  アプリの新しい版も届く）。見ていた画面は sessionStorage（pull-view）に覚えて、読み直したあとに戻す。
  書きかけの入力（初期値から変わった欄・選んだ写真）や開いているポップアップ（.overlay）が
  あるときは、消えてしまうので更新しない
- 無料プランは1週間ほとんどアクセスがないと一時停止される。判定は投稿ではなく
  データベースへのリクエスト。アプリを開くだけで足りる。止まる1週間前に警告メールが届き、
  止まってもデータは消えず、ダッシュボードから再開できる
- 無料枠は DB 500MB・写真 1GB・通信 月5GB。文字は何件書いても埋まらない。
  写真が1枚約370KBなので、埋まるとしたら写真のほう（1GB で約2,700枚）。
  近くなったら一覧用の小さい写真を別に持たせる
